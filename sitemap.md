# サイトマップ

- ドキュメント区分: DOCUMENT 04
- 作成者: （氏名を入力）
- バージョン: Ver 1.0

## 詳細ドキュメント（Google スプレッドシート）

このページは概要のみを記載しています。詳細なサイトマップは以下のリンクから確認してください。

- 入力想定URL: `https://docs.google.com/spreadsheets/d/xxxxx`

## 目次

1. サイト構造図
2. ページ一覧
3. ユーザーフロー
4. URL設計

## 1. サイト構造図

### Webサイト構造図（必須）

トップページ（`/`）
├─ ヘッダー
│  ├─ ロゴ
│  ├─ 特徴（`#features`）
│  ├─ 使い方（`#how`）
│  ├─ こんな人おすすめ（`#persona`）
│  ├─ おすすめ（`#cafes`）
│  └─ カフェを探す（`#search`）
├─ ヒーロー/検索導線（`#search`）
│  ├─ 地図表示（Leaflet + OpenStreetMap）
│  ├─ 検索UIイメージ
│  └─ 条件チップ（Wi-Fi/電源/静か/長時間OK）
├─ 特徴セクション（`#features`）
├─ 使い方セクション（`#how`）
├─ ターゲットユーザーセクション（`#persona`）
├─ カフェ表示例セクション（`#cafes`）
├─ CTAセクション
└─ フッター

### Webアプリケーション構造図（必須）

静的フロントエンドアプリケーション
├─ トップ画面（`/`）
│  ├─ セクション内ナビゲーション（ハッシュ遷移）
│  ├─ カフェ検索導線UI
│  └─ マップ表示
└─ 地図コンポーネント
   ├─ 地図初期化（上野周辺中心）
   ├─ タイル表示（OpenStreetMap）
   └─ カフェマーカー表示（3件・ポップアップ付き）

## 2. ページ一覧

### Webサイト ページ一覧（必須）

| No. | ページ名 | ページの目的 | 主要コンテンツ |
|---|---|---|---|
| 1 | トップページ | サービス紹介とカフェ検索導線の提供 | ヒーロー、検索UI、地図、特徴、使い方、ターゲットユーザー、カフェ表示例、CTA |

### Webアプリ 画面一覧（必須）

| No. | 画面名 | 画面の目的 | 主要機能 |
|---|---|---|---|
| 1 | トップ画面 | サービス概要表示と主要導線提供 | セクションナビゲーション、アンカー遷移、CTA |
| 2 | 地図表示エリア | 上野周辺の作業向けカフェ情報を可視化 | Leaflet地図表示、OSMタイル描画、3マーカー表示、ポップアップ表示 |

## 3. ユーザーフロー

### メインシナリオ（必須）

1. ユーザーがトップページ（`/`）へアクセス
2. ヒーロー内の地図と条件チップで用途を把握
3. 「この条件で探す」またはナビゲーションから「おすすめ（`#cafes`）」へ移動
4. カフェ表示例を確認
5. マップ上のマーカーをクリックして詳細（店名・評価・特徴）を確認

### サブシナリオ（任意）

#### サブシナリオ①

1. ユーザーがヘッダーナビゲーションの「特徴（`#features`）」を選択
2. サービスの強み（条件検索/地図検索/レビュー）を確認
3. 「カフェを探す（`#search`）」に戻って検索導線へ遷移

#### サブシナリオ②

1. ユーザーが「こんな人おすすめ（`#persona`）」を閲覧
2. 想定ユーザー像と課題を確認
3. 「おすすめ（`#cafes`）」へ遷移して表示例を確認

## 4. URL設計

### URL一覧（必須）

| URL | HTTPメソッド | 画面/機能 | 説明 |
|---|---|---|---|
| `/` | GET | トップページ | ランディングページ全体を表示 |
| `/#search` | GET | ヒーロー/検索導線 | 検索導線セクションへスクロール |
| `/#features` | GET | 特徴セクション | サービス特徴セクションへスクロール |
| `/#how` | GET | 使い方セクション | 使い方セクションへスクロール |
| `/#persona` | GET | ターゲットユーザーセクション | 想定ユーザー紹介セクションへスクロール |
| `/#cafes` | GET | カフェ表示例セクション | カフェカード表示セクションへスクロール |

## 補足（照合結果）

- 現行実装は静的1ページ構成で、複数URLページ（例: `/cafes`, `/contact`）は未実装です。
- 地図機能は Leaflet + OpenStreetMap で実装済みです（3マーカー表示）。
- 画像運用方針として、公開時は必要に応じてモザイク（ぼかし/ピクセル化）を適用します。
